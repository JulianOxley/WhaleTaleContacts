# State

(Fill in per this project: where state lives — a single store, a backend, both — what it holds,
what it deliberately does not hold, persistence behaviour, and what happens on first visit /
first run with no existing state.)
