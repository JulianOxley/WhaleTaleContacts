# Standards

Referenced from `AGENTS.md`, which is the entry point and stays short. Nothing here restates a
requirement — requirements live in Tracker Stub.

| Page                        | What it covers                                                          |
| ---------------------------- | ------------------------------------------------------------------------ |
| `engineering-standards.md`   | Stack, layout, the domain/UI split, testing, what the suite cannot see  |
| `state.md`                   | The store, what it holds and does not hold, persistence                |
| `git-and-releases.md`        | Branches, commits, the hooks, the pipeline, how versions are cut        |
| `plan-policy.md`             | Whether plans are archived or ephemeral in this repo, and why           |
